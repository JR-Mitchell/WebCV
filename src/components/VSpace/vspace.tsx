//Imports from React module
import React from 'react';

/**
 * Basic <VSpace> component. Is just a vertical space.
 */
function VSpace(props:{children?:React.ReactNode}) {
    return <br />
}

export default VSpace;
