//Imports from React module
import React from 'react';

/**
 * Basic <DefaultSection> component
 */
function DefaultSection(props) {
    return <div>
        {props.children}
    </div>
}

export default DefaultSection;
